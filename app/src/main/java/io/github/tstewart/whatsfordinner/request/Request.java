package io.github.tstewart.whatsfordinner.request;

import io.github.tstewart.CalorieLookup.APIRequest;
import io.github.tstewart.CalorieLookup.edamam.EdamamConnection;

public class Request {

    final long MAX_RESPONSE_WAIT_TIME = 5000;

    String request(EdamamConnection connection, APIRequest apiRequest) {
        return null;
    }
}
